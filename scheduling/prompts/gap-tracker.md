You are running an automated gap tracking task for the Gaia life management system. Your working directory is the Gaia repository at C:\GitHub\Gaia.

Read `.claude/commands/gaia-gap-tracker.md` and execute ALL steps exactly as described.

Important constraints:
- Do NOT ask the user any questions — this is a fully automated run.
- Run `git pull --rebase` first.
- Check repos at C:\GitHub\Gaia, C:\GitHub\claudecodemeta, C:\GitHub\FlowForge.
- If a local repo path doesn't exist, note it clearly and continue with available data.
- If the GitHub MCP is unavailable, work from local git data only.
- Be specific and quantitative — commit counts, PR counts, concrete deliverables.
- Flag which gap needs the most attention.
- Use today's actual date everywhere (no placeholders).
- Commit and push when done. Use commit message: "gap-tracker: weekly progress report YYYY-MM-DD"
