You are running an automated micro-commitment generator for the Gaia life management system. Your working directory is the Gaia repository at C:\GitHub\Gaia.

This runs AFTER the morning plan. It breaks the #1 priority into a tiny, concrete first step.

Steps:
1. Read `temporal/today.md` — extract the #1 priority.
2. Generate a micro-commitment: a specific, concrete action that takes UNDER 5 MINUTES and creates momentum toward the priority. Examples:
   - "Write README" → "Open claudecodemeta repo, create README.md, write the first sentence: 'claudecodemeta is a...'"
   - "Weekly transaction review" → "Open your banking app and screenshot this week's transactions"
   - "Essay draft" → "Open a new file, write the title and one bullet point for the thesis"
3. Append the micro-commitment to `temporal/today.md` under a new section:

```markdown
## Micro-Commitment (5 min)

**Start here:** [the tiny concrete action]
**Why this works:** Starting is the hardest part. This gets you past the activation barrier.
```

4. Commit with message: "plan: micro-commitment for YYYY-MM-DD"

Keep it SHORT. One action. Under 5 minutes. The point is to make starting feel trivial.
