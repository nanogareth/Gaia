# Session Handover — 2026-03-14 #1

## Status

- **Branch:** main
- **Last commit:** `5b32367` feat: add Cowork task setup prompt for automated registration
- **Uncommitted changes:** none (clean tree)
- **Tests:** N/A (state-only repo)

## Overarching Context

Gareth is building a programme to demonstrate deep expertise with Anthropic's tools for a Design Engineer role at Anthropic Education Labs. Three projects form the **Core Triad**, all to be open-sourced as portfolio evidence:

| Project | Role | Repo |
|---------|------|------|
| **Gaia** | Coordination layer — schedules work across all life domains, orchestrates planning/reflection, tracks state | `C:\GitHub\Gaia` |
| **claudecodemeta** | Cross-interface memory — persistent corrections, user profiles, shared state across Claude Code / Claude.ai / Cowork | `C:\GitHub\claudecodemeta` |
| **FlowForge** | Context bootstrapping — sets up CLAUDE.md, hooks, MCPs, sandbox, Gaia registration for new projects | `C:\GitHub\FlowForge` |

Gaia is evolving from passive state tracking to an **active coordination layer** that decides what Claude Code should work on next, provisions context, and captures results. The scheduling layer (Layer 5) is central to this — it automates the daily rhythm (morning plan, evening reflect, weekly review, gap tracker) and manages subscription utilisation across 5-hour rolling windows.

See `vision-anthropic-programme.md` and `project-briefs-core-triad.md` for full programme design and detailed briefs.

## What Was Done

- Updated CLAUDE.md and `Gaia_Holistic_Life_Ecosystem.md` to reflect current state (removed phase-future language, fixed SessionEnd hook status, updated file tree/manifest/MCP table/roadmap, added Obsidian plugin section, new design decisions)
- Added `vision-anthropic-programme.md` and `project-briefs-core-triad.md` — reframed Gaia as coordination layer, defined Core Triad, Cowork scheduling, Phase 5 roadmap
- Created `domains/anthropic-application.md` — gap analysis tracking for 5 areas (React/TS depth, frontend polish, consumer scale, public portfolio, published thinking)
- Created `/gaia-gap-tracker` command — weekly progress report checking core triad repos
- Updated `/gaia-plan` command — added usage-aware work queue with 3 windows and message budgets
- Wired **SessionEnd hook** in `~/.claude/settings.json` — auto-syncs project sessions to Gaia state
- Created 4 Cowork scheduled tasks via setup prompt (morning plan, evening reflect, weekly review, gap tracker)
- Enabled `coworkScheduledTasksEnabled: true` in Claude Desktop config

## Decisions Made

- Gaia reframed from "state-only" to "state-and-coordination" repo
- Core Triad: three independent projects sharing state via filesystem, may converge later
- SessionEnd hook uses absolute path for reliability in global scope
- Sonnet for routine Cowork tasks, Opus for weekly review (needs synthesis)
- `journal/` is gitignored intentionally — synced via Obsidian Git only

## Key Discoveries

- **Cowork SKILL.md path** is `D:\OneDrive\Documents\Claude\Scheduled\<task-name>\SKILL.md`, NOT `~/.claude/scheduled-tasks/` (saved to memory)
- SKILL.md files are NOT auto-discovered — must create tasks via UI, they generate SKILL.md
- Task metadata (schedule, model, folder) stored in Electron internal storage, not editable files

## Open Issues

### CRITICAL: Scheduling Layer Needs Redesign

Cowork is **not fit for purpose** as sole scheduling backbone. Two severe limitations:

1. **No programmatic task creation** — Tasks only created via UI or `/schedule` paste. Gaia cannot self-provision or update its own tasks.
2. **Requires machine awake** — Tasks only fire while Claude Desktop is open and machine is awake. Missed runs get one catch-up (most recent only, within 7 days). Unreliable for daily rhythm.

### Other Open Issues

- **Test task** at `D:\OneDrive\Documents\Claude\Scheduled\gaia-test-task\` — may not be registered (created via file). Clean up.
- **`gaia-tasks`** — initial task from Cowork paste session, may be redundant. Review.

## Next Steps (Prioritised) — STATUS

1. ~~**Research Claude Code CLI non-interactive invocation**~~ — **DONE (session #2).** `claude -p` works headlessly. Key flags: `--model`, `--max-budget-usd`, `--dangerously-skip-permissions`. Binary at `~/.local/bin/claude` v2.1.76.
2. ~~**Design scheduling layer v2**~~ — **DONE (session #2).** Windows Task Scheduler + Claude Code CLI chosen. Implementation in `scheduling/` directory: `run-task.sh` wrapper, 4 prompt files, `setup-scheduler.ps1` for registration.
3. ~~**Clean up Cowork artifacts**~~ — **DONE (session #2).** `gaia-tasks` and `gaia-test-task` marked deprecated in SKILL.md. 4 real Cowork tasks kept as supplementary.
4. ~~**Update docs**~~ — **DONE (session #2).** CLAUDE.md and vision doc updated to reflect Task Scheduler approach.
5. **Continue Anthropic programme** — remaining Week 0 items addressed in session #2 (see below).
